import React from 'react';
import { Card, CardContent } from '../card';
const TeamCard = (): React.JSX.Element => {
  return (
    <Card>
      <CardContent className="flex items-center gap-4"></CardContent>
    </Card>
  );
};
